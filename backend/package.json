{
  "name": "clariword-backend",
  "version": "1.0.0",
  "description": "Reference backend for the ClariWord AI Chrome extension. Holds the AI provider key so the extension never has to.",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "start:local": "node --env-file=.env server.js",
    "smoke": "node smoke.js"
  }
}
